Steps to run the project:
1) git clone "repository-link"
2) python -m venv .venv (Create a virtual environment)
3) .\.venv\Scripts\activate (Setting the virtual environment)
4) pip install pygame (To install pygame libray)
5) python main.py (To run the actual file)
